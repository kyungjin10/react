// 1. Number Type
let num1 = 27;
let num2 = 1.5;
let num3 = -20;

// console.log(num1 + num2);

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;

// 2. String Type
let myName = '김경진';
let myLocation = '역삼';
let introduce = myName + myLocation;

let introduceText = `${myName}은 ${myLocation}에 거주합니다`;
console.log(introduceText);

// 3. Boolean Type
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type (아무것도 없다)
let empty = null;

// 5. Undefined Type -> 존재하지 않은 값을 불러오려고 할 때
let none;
console.log(none);